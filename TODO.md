# TaxPearls TODO

更新日期：2026-09-22  
进度基线：[`docs/02-产品需求文档.md`](docs/02-产品需求文档.md) v1.3

## 使用规则

本文件只追踪未完成工作、阶段验收门和必要的完成证据，不重复 PRD 的完整需求说明。

状态口径：

- `✅ 已验收`：代码、自动化测试及该项要求的运行环境证据均已通过。
- `🧪 待复验`：实现已存在，但须在当前代码上重新执行验收。
- `🔨 部分实现`：只有部分能力落地，不得按完成统计。
- `⬜ 未开始`：尚无符合需求的实现。
- `🚧 外部验收`：实现侧无法单独给出结论，需要真实人员、数据、服务或部署环境。
- 文档、源码、自动化、浏览器、PDF/部署、真实用户验收是不同证据层，不互相替代。

## 一、P1 阶段验收门

本节严格对应 PRD 7.3。五项工程门全部通过，才可表述为“P1 核心工程闭环”；第 6 项通过后，才可表述为“P1 外部验收完成”。

| ID | 状态 | 验收项 | 完成定义 | 本轮证据 |
| --- | --- | --- | --- | --- |
| P1-GATE-01 | ✅ 已验收 | 规则库成型 | 规则不少于20条；内部勾稽、外部交叉、指标偏离三类均有覆盖；规则校验及全部标注样例通过 | 24条、三类覆盖；96/96标注样例通过 |
| P1-GATE-02 | ✅ 已验收 | 单户完整审计 | 浏览器上传标准材料，规则执行并显示证据，历史结果可重开，HTML/PDF均可生成；总时长小于5分钟 | 全新数据库浏览器上传至证据页约22秒；A4 PDF单独生成3.488秒、1,350,977字节；重启后可重开 |
| P1-GATE-03 | ✅ 已验收 | 实训自动评分 | 教师可发布仿真案例；学生可查账、提交；评分明细必须分别列出正确、漏检、误报和原因；教师可复核调整 | 浏览器实测发布、查账、提交；结果20分并列出3项漏检、1项误报及原因；教师调整为88分 |
| P1-GATE-04 | ✅ 已验收 | 真实鉴权与角色隔离 | 无默认明文密码；Argon2id；教师、学生、机构管理员、会计、平台管理员权限生效；机构/客户越权被拒绝；会计导出二次确认 | 58项全量回归中包含Argon2id、令牌哈希、五角色、机构/客户边界、越权拒绝及会计导出409二次确认 |
| P1-GATE-05 | ✅ 已验收 | 数据持久化与复现 | 服务重启后审计快照仍存在；历史结果保存当时的标准指标、规则定义、Finding和版本，刷新不丢 | 浏览器流程中停止并重启服务后，原会话、企业指标、风险结果及历史审计均恢复；数据库重开测试通过 |
| P1-GATE-06 | 🚧 外部验收 | 自动评分与教师人工评分一致率 | 真实教师对同批样本人工评分，与系统评分一致率不低于90%，保留样本、评分明细、统计方法及签字/确认记录 | 缺真实教师与同批评分样本；不得用开发者自评替代 |

## 二、P1 尚未完成的功能需求

这些项目不阻断 PRD 7.3 的五项核心工程门，但仍属于 P1 功能范围；在完成前，不得表述为“全部 P1 FR 已完成”。

| 优先级 | 状态 | FR | 待办 | 完成定义与验收方法 |
| --- | --- | --- | --- | --- |
| P1 | 🔨 部分实现 | FR-A06 | 补齐多期间自动归集 | 在现有历史/年度指标基础上支持同比、环比和滚动12月自动归集；覆盖期间断档、重复期、跨年和零基数测试 |
| P1 | 🔨 部分实现 | FR-A07 | 补齐发票 XML 与来源格式适配 | 除统一 Excel 明细外，至少完成已确定来源的 XML/Excel 映射；红字、作废、重复票号和税额口径回归通过 |
| P1 | 🔨 部分实现 | FR-A08 | 补齐银行流水来源适配与调节工作流 | 支持已确定银行导出格式；原始流水、分类、调节项和权责口径分层留痕，禁止把流水合计直接当收入 |
| P1 | 🔨 部分实现 | FR-B10 | LLM 解读层 | 已有图谱问答、证据节点引用校验、无配置降级和越界回答拒绝；仍须连接选定的真实Provider，完成命中Finding白话解读的真实调用验收 |
| P1 | 🔨 部分实现 | FR-B11 | 规则参数编辑与试跑 | 在现有列表/启停上增加受校验的参数编辑、版本提示和指定材料试跑；非法公式、未知指标与越权修改必须拒绝 |
| P1 | ⬜ 未开始 | FR-D04 | 报告模板配置 | 机构可配置 Logo、抬头和页脚声明；HTML 与 PDF 同步生效；文件类型、尺寸、XSS和跨机构隔离通过 |
| P1 | ⬜ 未开始 | FR-D05 | LLM 总体结论与建议 | 只能汇总 Finding；段落逐项标注来源规则；与 FR-B10 共用安全边界并覆盖超时、无密钥和格式错误 |
| P1 | 🔨 部分实现 | FR-G07 | 补齐客户（企业）档案管理界面 | 机构管理员/平台管理员可在界面新增客户企业、指派本机构会计、查看本机构客户清单；无权限角色拒绝、跨机构不可见；新建企业可在工作台下拉选中并参与审计。**本期不做删除**（企业下挂审计记录，硬删产生孤儿数据，须另行设计归档语义） |
| P1 | 🔨 部分实现 | FR-G08 | 部署与运维闭环 | Dockerfile、Compose、外置配置、持久卷、健康检查和日志已具备；**已实测：目标环境镜像构建、容器启动、TLS 入口、数据持久化、升级重建**；仍须补备份恢复与回滚演练 |

## 三、P0安全与生产部署遗留

| 优先级 | 状态 | FR | 待办 | 完成定义与验收方法 |
| --- | --- | --- | --- | --- |
| 阻断生产 | ✅ 已验收 | FR-F01 | TLS 1.2+ | 在实际入口验证 HTTPS、HTTP跳转、证书链和弱协议禁用；本机 `127.0.0.1` 不算通过。**2026-09-22 外部实测通过**：HTTPS 200 且证书校验结果 0；HTTP 301 跳转至 8443；TLS 1.0/1.1 被拒、1.2/1.3 可用 |
| 阻断敏感数据 | ⬜ 未开始 | FR-F03 | 敏感字段列级加密 | 先确认需落库字段，再实现密钥外置、轮换和解密权限；验证数据库明文不可见 |
| 阻断生产 | 🚧 外部验收 | FR-F05 | 环境隔离 | 明确演示/培训/生产的主机、账号、数据库和密钥边界，并进行跨环境不可见验证 |
| P2设计门 | ⬜ 未开始 | FR-F07 | 多租户行级隔离 | 在P2多租户实现前完成租户键、查询约束、后台任务与导出链路的统一设计 |

## 四、P2待办

P2 尚未进入阶段验收，不与本轮 P1 验收混算。FR-A09 有界多文件/ZIP导入已提前落地；扫描页AI候选提取已有受限实现和模拟协议回归，但尚未完成真实Provider的OCR兜底演示。其余按PRD顺序包括：社保/公积金、合同台账、RPA、关联方图规则、规则版本生效期、站内通知、增量扫描、批量报告、报告水印、自动出题器、班级/作业管理、学情统计、个人画像、错题本、多租户、备份销毁策略。当前“规则—指标—法条—证据”知识图谱用于解释与导航，不等同于FR-B09关联方图规则引擎。

启动 P2 前必须先关闭本文件第二节中会影响数据契约、规则版本或部署架构的 P1 项，或由产品评审明确接受其延期影响。

## 五、固定验收命令

```powershell
# 全量单元与集成回归
.venv/Scripts/python.exe -X utf8 -m unittest discover -s tests -v

# 24条规则、96个标注样例
.venv/Scripts/python.exe -X utf8 scripts/check_rules.py

# Web 工作台
.venv/Scripts/python.exe -X utf8 -m webapp
```

浏览器验收必须使用全新临时数据库，覆盖首次初始化、登录、上传、筛选、历史快照、教师发布、学生查账与提交、教师复核、规则管理、操作日志和退出；验收后删除本轮临时数据库和浏览器临时产物。

## 六、本轮验收记录

### 2026-09-22 · 生产部署上线（外部环境：目标 VPS）

- **部署**：`docker compose up -d --build` 构建镜像 2.17GB，容器 `healthy`；数据库落在命名卷 `taxpearls_taxpearls-data`。
- **入口**：Caddy 2.11.4 反向代理，Let's Encrypt 证书自动签发（有效期至 2026-12-20，自动续期）；因宿主 443 端口被既有服务占用，HTTPS 走 8443，HTTP 301 正确跳转至 8443。
- **TLS 实测（外部网络）**：TLS 1.0 / 1.1 被拒绝，1.2 / 1.3 可用，证书校验通过 → **FR-F01 验收通过**。
- **持久化**：以本地库整体替换线上空库后重启容器，14 家企业 / 21 次审计记录完整保留；容器内**读、写**均单独验证正常。
- **升级路径**：修改前端静态资源后重建镜像并重启，业务数据不受影响。
- **鉴权回归**：未登录访问 `/api/clients` 返回 401。

结论：FR-G08 的「目标环境构建 / 容器启动 / TLS 入口 / 数据持久化 / 升级重建」实测通过；**备份恢复与回滚演练仍未执行**，FR-G08 维持 🔨。

### 2026-09-21 · 本地工作区（全新临时数据库）

在当前工作区、全新临时数据库上完成：

- `unittest`：58项全部通过；覆盖材料解析、AI提取契约、知识图谱引用、规则引擎、仪表盘、鉴权、持久化与实训；新增回归覆盖“纯合成测试”案例发布、机构管理员越权建教师、非负责人会计访问、负责人会计导出二次确认。
- 规则核对：24条规则、96个标注样例，96通过、0失败。
- 浏览器：完成首次初始化、管理员创建教师/学生、上传并核对合成Excel、24条规则审计、工作台与证据页、筛选、历史快照、知识图谱、教师发布、学生查账并提交、漏检/误报解释、教师人工复核；最终稳定版本控制台0 error。
- 持久化：审计完成后停止并重启服务，原登录会话、工作台指标、风险摘要与审计记录仍可读取。
- 性能：浏览器选取材料至证据页约22秒；同一案例A4 PDF生成3.488秒，1,350,977字节；均低于P1的5分钟门槛。
- 验收中修复：教学数据判定在审计与发布作业之间不一致，导致名称含“纯合成测试”的合法案例不能发布；现已统一判定并补回归。

结论：P1五项核心工程门全部通过；P1-GATE-06仍需真实教师同批评分，一致率达到90%后才能宣布P1外部验收完成。第二节所列P1 FR仍未全部完成，不能表述为“全部P1功能完成”。
