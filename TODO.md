# TaxPearls TODO

更新日期：2026-09-22  
进度基线：[`docs/02-产品需求文档.md`](docs/02-产品需求文档.md) v1.10

## 使用规则

本文件只追踪未完成工作、阶段验收门和必要的完成证据，不重复 PRD 的完整需求说明。

状态口径：

- `✅ 已验收`：代码、自动化测试及该项要求的运行环境证据均已通过。
- `🧪 待复验`：实现已存在，但须在当前代码上重新执行验收。
- `🔨 部分实现`：只有部分能力落地，不得按完成统计。
- `⬜ 未开始`：尚无符合需求的实现。
- `🚧 外部验收`：实现侧无法单独给出结论，需要真实人员、数据、服务或部署环境。
- 文档、源码、自动化、浏览器、PDF/部署、真实用户验收是不同证据层，不互相替代。

## P1 进度摘要

- **核心工程门：5/5 已通过**。规则、单户审计、实训评分、鉴权隔离、持久化均有当前自动化与浏览器证据。
- **P1 功能需求：26/28 已完成（93%）**。本轮完成 FR-B10；剩余 2 项为 LLM 总体结论和运维备份/回滚演练。
- **外部验收门：0/1（暂缓）**。P1-GATE-06 仍须由真实教师对同批样本评分；按当前安排暂不组织，不阻塞 FR-D05、FR-G08 的开发与工程验收，但在完成前仍不能宣布“P1 全部完成”。

## 一、P1 阶段验收门

本节严格对应 PRD 7.3。五项工程门全部通过，才可表述为“P1 核心工程闭环”；第 6 项通过后，才可表述为“P1 外部验收完成”。

| ID | 状态 | 验收项 | 完成定义 | 本轮证据 |
| --- | --- | --- | --- | --- |
| P1-GATE-01 | ✅ 已验收 | 规则库成型 | 规则不少于20条；内部勾稽、外部交叉、指标偏离三类均有覆盖；规则校验及全部标注样例通过 | 24条、三类覆盖；96/96标注样例通过 |
| P1-GATE-02 | ✅ 已验收 | 单户完整审计 | 浏览器上传标准材料，规则执行并显示证据，历史结果可重开，HTML/PDF均可生成；总时长小于5分钟 | 全新数据库浏览器上传至证据页约22秒；A4 PDF单独生成3.488秒、1,350,977字节；重启后可重开 |
| P1-GATE-03 | ✅ 已验收 | 实训自动评分 | 教师可发布仿真案例；学生可查账、提交；评分明细必须分别列出正确、漏检、误报和原因；教师可复核调整 | 浏览器实测发布、查账、提交；结果20分并列出3项漏检、1项误报及原因；教师调整为88分 |
| P1-GATE-04 | ✅ 已验收 | 真实鉴权与角色隔离 | 无默认明文密码；Argon2id；教师、学生、机构管理员、会计、平台管理员权限生效；机构/客户越权被拒绝；会计导出二次确认 | 81项全量回归中包含Argon2id、令牌哈希、五角色、机构/客户边界、规则试跑/保存越权拒绝及会计导出409二次确认 |
| P1-GATE-05 | ✅ 已验收 | 数据持久化与复现 | 服务重启后审计快照仍存在；历史结果保存当时的标准指标、规则定义、Finding和版本，刷新不丢 | 浏览器流程中停止并重启服务后，原会话、企业指标、风险结果及历史审计均恢复；规则配置重启后仍为v2.1，旧审计保持v2.0，新审计使用v2.1 |
| P1-GATE-06 | 🚧 外部验收（暂缓） | 自动评分与教师人工评分一致率 | 真实教师对同批样本人工评分，与系统评分一致率不低于90%，保留样本、评分明细、统计方法及签字/确认记录 | 2026-09-22 按产品安排暂缓，待 FR-D05、FR-G08 收尾后再组织；不得用开发者自评替代 |

## 二、P1 尚未完成的功能需求

这些项目不阻断 PRD 7.3 的五项核心工程门，但仍属于 P1 功能范围；在完成前，不得表述为“全部 P1 FR 已完成”。

| 优先级 | 状态 | FR | 待办 | 完成定义与验收方法 |
| --- | --- | --- | --- | --- |
| P1 | ⬜ 未开始 | FR-D05 | LLM 总体结论与建议 | 只能汇总 Finding；段落逐项标注来源规则；与 FR-B10 共用安全边界并覆盖超时、无密钥和格式错误 |
| P1 | 🔨 部分实现 | FR-G08 | 部署与运维闭环 | Dockerfile、Compose、外置配置、持久卷、健康检查和日志已具备；**已实测：目标环境镜像构建、容器启动、TLS 入口、数据持久化、升级重建**；仍须补备份恢复与回滚演练 |

### 本轮移出待办

| 优先级 | 状态 | FR | 已完成能力 | 验收证据 |
| --- | --- | --- | --- | --- |
| P1 | ✅ 已验收 | FR-B10 | 仅对已命中的冻结 Finding 生成业务白话、触发原因和核对顺序；服务端锁定 `hit` 与规则引用，证据哈希缓存并持久化，模型不参与判定 | 真实 `deepseek-flash` 调用通过；R-001 返回同规则引用和 4 条核对步骤；非命中、越权、错误 verdict/引用/结构均被拒绝；浏览器显示锁定结论并复用已保存解读，控制台 0 error/0 warning；81 项回归通过 |
| P1 | ✅ 已验收 | FR-A08 | 接入建设银行企业账户明细公开字段口径及通用收支/单金额方向表头；原始流水、逐笔分类、底稿编号、价税拆分和现金/跨期调节分层；只有全部收入流水完成复核才生成权责口径不含税收入 | 唯一流水号跨文件去重、人民币/期间/收支边界、未分类/未复核/错误引用/非经营误认收入拒绝通过；Web 展示 3 笔原始流水和 4 条调节底稿，R-015 以 1,030,000 对 1,030,000 通过，服务重启可重开，控制台 0 error/0 warning |
| P1 | ✅ 已验收 | FR-A07 | 支持财政部电子凭证会计数据标准数电发票 XBRL XML、统一模板及来源 Excel 表头映射；多文件/ZIP 合并后按票号统一去重，生成销项/采购不含税净额、税额净额及本期确认抵扣税额 | 财政部推广应用版 2 份官方实例实读通过；红字冲减、作废剔除、价税勾稽、跨文件重复、跨期、DTD/实体及非标准 XML 拒绝通过；未确认用途采购税额不计抵扣，Web 上传、证据来源、重启持久化和控制台 0 error 通过 |
| P1 | ✅ 已验收 | FR-A06 | `历史指标`兼容原成品指标与标准期间序列，自动生成同比、环比、滚动12月及连续年度规则输入；支持月/季/半年/年度和跨文件合并 | 断档不做部分滚动汇总，重复/非法期间及本期冲突被拒绝，零基数保留比较值但不伪造增长率，发生额与期末余额分开处理；跨年、年度规则、Web 上传、证据来源、重启持久化及浏览器控制台 0 error 通过 |
| P1 | ✅ 已验收 | FR-B11 | 无代码结构化参数编辑、版本提示、指定历史材料试跑与新版本保存；试跑不改历史审计，新版本只影响后续审计 | 非法逻辑、未知指标、版本不递增、过期版本和越权请求均被拒绝；教师/机构管理员/会计仅能按审计访问范围试跑，平台管理员可保存；v2.0旧审计、v2.1新审计及重启持久化经自动化和浏览器实测通过 |
| P1 | ✅ 已验收 | FR-D04 | 机构名称、报告标题、页脚和 PNG/JPEG Logo 可在管理页配置，并同步到机构所属审计的 HTML/PDF 报告 | 旧库迁移、类型/大小/像素限制、图片重编码、XSS 转义、权限与跨机构隔离自动化通过；全新库浏览器配置、重启恢复、HTML 展示、PDF 下载及控制台 0 error 通过 |

## 三、P0安全与生产部署遗留

| 优先级 | 状态 | FR | 待办 | 完成定义与验收方法 |
| --- | --- | --- | --- | --- |
| 阻断生产 | ✅ 已验收 | FR-F01 | TLS 1.2+ | 在实际入口验证 HTTPS、HTTP跳转、证书链和弱协议禁用；本机 `127.0.0.1` 不算通过。**2026-09-22 外部实测通过**：HTTPS 200 且证书校验结果 0；HTTP 301 跳转至 8443；TLS 1.0/1.1 被拒、1.2/1.3 可用 |
| 阻断敏感数据 | ⬜ 未开始 | FR-F03 | 敏感字段列级加密 | 先确认需落库字段，再实现密钥外置、轮换和解密权限；验证数据库明文不可见 |
| 阻断生产 | 🚧 外部验收 | FR-F05 | 环境隔离 | 明确演示/培训/生产的主机、账号、数据库和密钥边界，并进行跨环境不可见验证 |
| P2设计门 | ⬜ 未开始 | FR-F07 | 多租户行级隔离 | 在P2多租户实现前完成租户键、查询约束、后台任务与导出链路的统一设计 |

## 四、P2待办

P2 尚未进入阶段验收，不与本轮 P1 验收混算。FR-A09 有界多文件/ZIP导入已提前落地；扫描页AI候选提取已有受限实现和模拟协议回归，但尚未完成真实Provider的OCR兜底演示。其余按PRD顺序包括：社保/公积金、合同台账、RPA、关联方图规则、规则版本生效期、站内通知、增量扫描、批量报告、报告水印、自动出题器、班级/作业管理、学情统计、个人画像、错题本、多租户、备份销毁策略。当前“规则—指标—法条—证据”知识图谱用于解释与导航，不等同于FR-B09关联方图规则引擎。

启动 P2 前必须先关闭本文件第二节中会影响数据契约、规则版本或部署架构的 P1 项，或由产品评审明确接受其延期影响。

## 五、固定验收命令

```powershell
# 全量单元与集成回归
.venv/Scripts/python.exe -X utf8 -m unittest discover -s tests -v

# 24条规则、96个标注样例
.venv/Scripts/python.exe -X utf8 scripts/check_rules.py

# Web 工作台
.venv/Scripts/python.exe -X utf8 -m webapp
```

浏览器验收必须使用全新临时数据库，覆盖首次初始化、登录、客户建档与会计指派、客户关联上传、筛选、历史快照、教师发布、学生查账与提交、教师复核、规则管理、操作日志和退出；验收后删除本轮临时数据库和浏览器临时产物。

## 六、本轮验收记录

### 2026-09-22 · FR-B10 命中 Finding 的 LLM 白话解读（本地全新临时数据库）

- **职责边界**：模型只接收已冻结的规则、确定性结论、计算过程、阈值、证据来源、法条和建议；只允许返回 `hit`、当前规则 ID、业务白话、触发原因及 1–5 条核对步骤。规则引擎结论在接口和界面中单独锁定，模型不能改判。
- **安全与失败语义**：服务端严格校验 JSON 字段、`verdict`、引用规则、文本长度和步骤数量；无配置返回 503，上游失败、截断、格式错误、改判或引用错位返回 502，非命中 Finding 返回 422，学生和无权审计访问被拒绝。密钥仅用于 Authorization，不进入提示、响应、数据库或审计日志。
- **可追溯与持久化**：以审计 ID、规则 ID 和冻结证据 SHA-256 保存一次有效解读；相同证据再次查看直接使用缓存，证据变化不会误用旧结果。自动化验证数据库重开后仍可读取，模型调用只发生一次。
- **真实 Provider**：使用项目当前配置的 `deepseek-flash` 对仿真样例 R-001 实际调用成功，返回 `hit`、引用 R-001、业务白话、触发原因和 4 条核对步骤并通过全部约束校验。
- **浏览器**：全新数据库完成初始化、上传仿真 Excel、展开 R-001、生成解读；页面同时保留确定性证据卡与“规则引擎结论已锁定：命中（R-001）”，再次点击显示“已保存解读”，控制台 0 error、0 warning。
- **自动化**：81 项 `unittest` 全部通过；新增覆盖真实 Finding 上下文、严格 verdict/引用校验、仅命中限制、学生权限、证据哈希缓存、单次调用和数据库重开。24 条规则、96 个标注样例全部通过。

结论：FR-B10 完成定义全部通过，已从“P1 尚未完成”移出。P1 核心工程门仍为 5/5；P1 功能需求现为 26/28，剩余 FR-D05、FR-G08；教师一致率仍是外部验收门。

### 2026-09-22 · FR-A08 银行来源适配与收入调节桥（本地全新临时数据库）

- **确定来源**：按建设银行公开企业网银手册展示的“唯一编号、交易日期、借/贷方发生额、余额、对方户名/账号、摘要”等字段接入来源 Excel，同时兼容交易流水号、收入/支出双列和“单金额 + 收支方向”表头；不声称覆盖未取得样本的全部银行版本。
- **原始层**：逐笔保留流水号、日期、收支、摘要、来源文件指纹和工作表行号；借方映射支出、贷方映射收入，只接受人民币。原始收入/支出分别汇总，但界面和证据明确标注“原始入账不直接作为收入”。账号只保留掩码提示，原始文件仍不落盘。
- **调节层**：新增 `银行调节` 底稿，逐笔关联收入流水并填写经营/借款/内部转账/资本金/往来等分类、权责所属期、本期不含税收入、底稿编号、复核状态和说明；现金、应收、跨期、退款折让等独立加减项另行记录。
- **完成约束**：只有全部收入流水具有唯一编号、恰好分类一次、全部已复核且底稿完整时，才生成 `银行.经营回款已复核不含税额`、`银行.权责口径调节额` 和 `银行.调节后不含税收入`。跨文件重复、外币、跨期流水、未分类、未复核、错误引用、非经营款误认收入及负数最终收入均拒绝。
- **浏览器**：全新库合并 3 笔来源流水与 4 条调节底稿；核对页展示原始收入 1,130,000、内部转账 200,000、支出 100,000，以及经营回款/内部转账/现金补计/退款扣减四层调节。R-015 以账面收入 1,030,000 对调节后不含税收入 1,030,000 通过，证据列出两份文件 SHA、逐行位置和 WP-BANK-001～004。服务重启后审计仍可重开，控制台 0 error、0 warning。
- **自动化**：79 项 `unittest` 全部通过；新增 5 项覆盖建设银行字段映射、单金额方向、千分位、来源账号掩码、原始/收入隔离、调节计算、R-015、跨文件重复、人民币/期间边界、未分类/未复核/误认收入拒绝、Web 提交和证据持久化。24 条规则、96 个标注样例全部通过。

结论：FR-A08 完成定义全部通过，已从“P1 尚未完成”移出。P1 核心工程门仍为 5/5；P1 功能需求现为 25/28，剩余 3 项；教师一致率仍是外部验收门。

### 2026-09-22 · FR-A07 发票 XML / 来源 Excel 适配（本地全新临时数据库）

- **确定来源**：XML 按财政部 2025 年《电子凭证会计数据标准——全面数字化的电子发票（推广应用版）》XBRL 元素解析；下载的 2 份官方专票/普票实例均实读通过。XML 解析只负责结构化取数，界面明确提示不代替电子税务局验真或数字签名验证。
- **Excel 映射**：保留统一 `发票明细` 模板，同时识别来源表中的数电票号码/开票日期/不含税金额或价税合计/税额/状态/购销双方税号/用途确认/进项转出等同义列；可由显式方向、工作表名或企业与购销方税号确定销项/采购。
- **净额与税额口径**：正常票正计、红字票按绝对金额冲减、作废票留痕但不计入；价税合计必须与不含税金额加税额在 0.01 元内勾稽。采购税额单独形成净额，只有明确确认抵扣且确认期落在审计期内的税额才进入 `凭证.本期确认抵扣税额`，进项转出同步扣减。
- **重复与边界**：同表及跨 XML/Excel 文件重复票号均拒绝；开票日期超出审计期、正常票负数、方向/状态不明、非财政部标准 XBRL、冲突字段、DTD/实体声明和超深/超量 XML 均拒绝。
- **浏览器**：全新库将现有审计材料与 XML 发票合并上传；核对页展示票号、日期、采购正常、不含税 500000 和税额 65000，审计指标由 8 项增至 11 项。服务重启后可从工作台重开相同审计，最终控制台 0 error、0 warning。
- **自动化**：74 项 `unittest` 全部通过；新增 6 项覆盖来源 Excel 映射、红字/作废、价税勾稽、用途确认与进项转出、财政部 XBRL XML、跨期、跨文件重复、安全限制、Web 提交和证据持久化。24 条规则、96 个标注样例全部通过。

结论：FR-A07 完成定义全部通过，已从“P1 尚未完成”移出。该次验收时 P1 功能需求为 24/28；当前总进度以本文件顶部摘要为准，教师一致率仍是外部验收门。

### 2026-09-22 · FR-A06 多期间自动归集（本地全新临时数据库）

- **输入兼容**：`历史指标`保留原有 `历史.* / 年度.*` 成品指标，同时接受利润表收入、成本、净利润和资产负债表资产总计的标准期间序列；本期报表与历史序列可在同一 Excel，也可分文件合并后归集。
- **期间语义**：支持完整自然月、季度、半年和年度；按同粒度生成上期、上年同期、环比率和同比率。2026-01 可正确跨年定位 2025-12 与 2025-01。
- **滚动与年度**：营业收入、营业成本等发生额只有在最近 12 个自然月全部存在时才生成滚动12月；资产总计等期末余额不跨期求和。完整年度序列自动生成 R-024 所需的三年净利润和三年末资产。
- **异常边界**：缺一个月份即不生成滚动12月；等价期间标签重复、无效期间、本期序列与本期报表冲突均明确拒绝；基期为 0 时保留真实比较值并让依赖规则按零基数语义未执行，不制造无穷大或 0% 增长率。
- **浏览器**：全新库上传含 24 条期间序列的 Excel，核对页展示收入滚动12月 1220、成本滚动12月 610、环比 0.2、同比 0.5；R-022 以 120 对 80 命中 50% 偏离，R-020 以两期 50% 毛利率正常通过，证据卡保留工作表行号、实际期间和来源。服务重启后审计仍可重开，控制台 0 error。
- **自动化**：68 项 `unittest` 全部通过；新增 6 项覆盖跨年同比/环比、滚动12月、断档、重复/非法期间、零基数、本期冲突、年度规则输入、单/多工作簿 Web 路径和证据持久化。24 条规则、96 个标注样例全部通过。

结论：FR-A06 完成定义全部通过，已从“P1 尚未完成”移出。该次验收时 P1 功能需求为 23/28；当前总进度以本文件顶部摘要为准，教师一致率仍是外部验收门。

### 2026-09-22 · FR-B11 规则参数编辑与试跑（本地全新临时数据库）

- **无代码参数编辑**：管理页按规则逻辑生成数值、方向和指标引用字段；指标只能从该规则声明的输入中选择，无需直接编写公式。
- **权限与数据边界**：平台管理员可保存新版本；教师、机构管理员和会计可调整草稿并在自己有权访问的审计上试跑；学生、跨机构教师和未获客户分配的会计均被拒绝。
- **校验与并发保护**：非法逻辑、未知或缺失指标、未递增版本均返回 422；以过期 `expected_version` 保存返回 409，避免覆盖他人已保存版本。
- **历史可复现**：R-001 阈值从 0.1 草稿改为 0.4 后，指定材料试跑由命中变为通过且原审计不变；保存 v2.1 后新审计使用 v2.1，旧审计仍显示并按 v2.0 复现。
- **持久化与界面**：停止并重启服务后，v2.1、阈值 0.4 和自定义状态仍存在；修复了“通过项”展开后仍被 CSS 隐藏的问题，浏览器可分别展开核对新旧审计版本，最终控制台 0 error。
- **自动化**：62 项 `unittest` 全部通过；新增覆盖平台管理员、机构管理员、教师、会计和学生权限，客户归属、非法参数、版本冲突、试跑无副作用、历史快照、新审计生效、重启持久化和审计日志；24 条规则、96 个标注样例全部通过。

结论：FR-B11 完成定义全部通过，已从“P1 尚未完成”移出。该次验收时 P1 功能需求为 22/28；当前总进度以本文件顶部摘要为准，教师一致率仍是外部验收门。

### 2026-09-22 · FR-D04 报告模板配置（本地全新临时数据库）

- **管理界面**：平台/机构管理员可编辑机构名称、报告标题、页脚声明，并上传或移除 Logo；普通学生修改与上传均返回 403。
- **图片安全**：Logo 仅接受静态 PNG/JPEG，输入最大 512KB、宽高不超过 1200 像素；服务端完整解码并重编码，SVG、超限与损坏文件均被拒绝，响应带 `nosniff` 且不公开缓存。
- **报告一致性**：配置同步用于 HTML 与真实 Chromium A4 PDF；PDF 包含图片对象，页脚模板先转义再交给 Chromium；报告和下载文件名均做安全处理。
- **机构隔离**：配置与 Logo 按机构保存；平台管理员查看其他机构审计时使用审计归属机构模板，不误用当前登录机构模板。
- **持久化与兼容**：旧数据库自动补齐 Logo 字段；停止并重启服务后，抬头、标题、页脚、Logo 与历史审计均可重开。
- **自动化与浏览器**：61 项 `unittest` 全部通过；24 条规则、96 个样例全部通过。浏览器完成配置、上传、审计、重启恢复、HTML 报告和 PDF 下载，最终控制台 0 error。

结论：FR-D04 完成定义全部通过，已从“P1 尚未完成”移出。该次验收时 P1 功能需求为 21/28；当前总进度以本文件顶部摘要为准，教师一致率仍是外部验收门。

### 2026-09-22 · FR-G07 客户（企业）档案管理（本地全新临时数据库）

- **界面与主链路**：平台/机构管理员可新增客户、指派本机构会计并查看客户清单；无审计客户会先出现在工作台企业下拉，点击“上传该客户材料”会自动带入客户档案。
- **一致性保护**：审计材料的纳税人识别号必须与所选客户一致；多份分别审计时禁止错误地共用一个客户档案。
- **权限与隔离**：学生调用客户接口返回 403；非负责人会计看不到客户及审计；跨机构客户不可见，跨机构负责人指派被拒绝。
- **自动化**：59 项 `unittest` 全部通过；新增覆盖客户建档、负责人名称、工作台预审计可见性、识别号冲突、跨机构/无权限拒绝、负责人审计访问及数据库重开。
- **浏览器**：全新库完成初始化 → 创建会计 → 客户建档并指派 → 工作台预审计展示 → 自动关联上传样例 → 生成审计 → 工作台展示风险；控制台 0 error。
- **持久化**：停止并重启服务后，登录会话、客户/负责人关系、关联审计及工作台风险均恢复；数据库核对确认审计记录 `client_id` 指向所建客户。

结论：FR-G07 本期完成定义全部通过，状态改为 ✅。删除仍按既定范围不实现；如后续需要，须另行设计归档/停用语义。

### 2026-09-22 · 生产部署上线（外部环境：目标 VPS）

- **部署**：`docker compose up -d --build` 构建镜像 2.17GB，容器 `healthy`；数据库落在命名卷 `taxpearls_taxpearls-data`。
- **入口**：Caddy 2.11.4 反向代理，Let's Encrypt 证书自动签发（有效期至 2026-12-20，自动续期）；因宿主 443 端口被既有服务占用，HTTPS 走 8443，HTTP 301 正确跳转至 8443。
- **TLS 实测（外部网络）**：TLS 1.0 / 1.1 被拒绝，1.2 / 1.3 可用，证书校验通过 → **FR-F01 验收通过**。
- **持久化**：以本地库整体替换线上空库后重启容器，14 家企业 / 21 次审计记录完整保留；容器内**读、写**均单独验证正常。
- **升级路径**：修改前端静态资源后重建镜像并重启，业务数据不受影响。
- **鉴权回归**：未登录访问 `/api/clients` 返回 401。

结论：FR-G08 的「目标环境构建 / 容器启动 / TLS 入口 / 数据持久化 / 升级重建」实测通过；**备份恢复与回滚演练仍未执行**，FR-G08 维持 🔨。

### 2026-09-21 · 本地工作区（全新临时数据库）

在当前工作区、全新临时数据库上完成：

- `unittest`：58项全部通过；覆盖材料解析、AI提取契约、知识图谱引用、规则引擎、仪表盘、鉴权、持久化与实训；新增回归覆盖“纯合成测试”案例发布、机构管理员越权建教师、非负责人会计访问、负责人会计导出二次确认。
- 规则核对：24条规则、96个标注样例，96通过、0失败。
- 浏览器：完成首次初始化、管理员创建教师/学生、上传并核对合成Excel、24条规则审计、工作台与证据页、筛选、历史快照、知识图谱、教师发布、学生查账并提交、漏检/误报解释、教师人工复核；最终稳定版本控制台0 error。
- 持久化：审计完成后停止并重启服务，原登录会话、工作台指标、风险摘要与审计记录仍可读取。
- 性能：浏览器选取材料至证据页约22秒；同一案例A4 PDF生成3.488秒，1,350,977字节；均低于P1的5分钟门槛。
- 验收中修复：教学数据判定在审计与发布作业之间不一致，导致名称含“纯合成测试”的合法案例不能发布；现已统一判定并补回归。

结论：P1五项核心工程门全部通过；P1-GATE-06仍需真实教师同批评分，一致率达到90%后才能宣布P1外部验收完成。第二节所列P1 FR仍未全部完成，不能表述为“全部P1功能完成”。
