# TaxPearls 重建版 TODO

更新日期：2026-09-26。基线是原远程 `2c5b060` 的实际源码，加上报告排版修正及旧本地工程增量。当前验收与阶段映射见 [README](README.md)；PRD、旧设计中的既往验收不代表新仓库已完成外部验收。

## 已完成的重建

- 原型 → 四规则 MVP → Web 与 24 条规则 → 多源材料 → 参数试跑/品牌/LLM 协议 → 总体叙述 → 人力/合同 → 登录加固/密码重置 → 运维与 RPA，按原工程的 16 条提交逐条重放，保留原合并关系；本次修复另作第 17 条提交。
- 重建阶段的独立 Python 环境、152 项测试及 96 个规则样例通过。原 17 提交基线另有浏览器上传、服务重启和 10 页报告目检记录；接回本地增量后的新验收见下方本轮记录。
- 旧客户测试夹具修正保留在原测试补充阶段，未与功能阶段合并；修复长阈值说明导致的 PDF 列挤压。

## 接回本地增量后的功能边界

| 功能 | 当前边界 |
| --- | --- |
| 完整自助注册与机构开户 | 已接回创始码邀请、邮箱验证及机构管理员注册；机构成员邀请与完整自助开户仍未完成，真实邮件未验收 |
| 关联方图风险规则 | 已接回 G-001 及主体/控制关系/异常交易路径；10 项专项测试覆盖校验、材料接入、快照和报告 |
| 规则版本生效起止日 | 已接回生效区间、完整规则版本快照及并发发布保护，跨区间审计会要求拆分 |
| 站内通知与邮件订阅 | 已接回通知、角色与归属校验、订阅同意、邮件队列及人工重试；14 项专项测试通过，真实邮件默认关闭 |
| 跨期风险比较（FR-C06） | 本轮已实现并验收：自动/手动基期、风险新增/消失/偏离恶化/改善、不可比保护、权限隔离和重启复现 |
| 报告归档与版本追溯（FR-D06） | 本轮已实现并验收：历史检索/分页、HTML 版本冻结、PDF 首次导出存档、版本清单、完整性核对及权限隔离 |
| 机构级多客户风险总览（FR-D07） | 本轮已实现并验收：期间/客户选择、冻结规则统计、机构 HTML/PDF、权限/迁移/备份恢复及重启字节一致 |
| 报告水印与原件核验（FR-D08） | 本轮已实现并验收：客户名/报告日期水印、逐页标识、登录后按权限核对冻结 HTML/PDF；不是数字签名或防转发保证 |
| 规则反向生成仿真账套（FR-E01） | 本轮已实现并验收：24 规则、三档幅度与种子；冻结完整答案、教师生成/发布、学生底稿/评分及不含答案的可重导入 Excel 下载 |
| 沙箱即时教学提示（FR-C07） | 本轮已实现并验收：风险标注后定位原始证据、差额/比例试算；缺项、零分母、单位和期间保护，不提前判答案或改变正式评分 |
| 班级 / 作业管理（FR-E08） | 本轮已实现并验收：班级名册、1–30 案例组卷与按分值汇总、草稿/发布/撤回、截止时间、教师管理与学生进度界面；18 项专项及浏览器/重启通过 |
| 其他 P2 能力 | 按下表逐项推进，不以本次重建等同于全量需求完成 |

## 当前 P2 逐项待办

以当前 PRD 的 **33 个 P2 相关需求 ID** 为清单：原 26 项（包括“P0 设计 / P2 实现”的 FR-F07）加上新增 H 模块 7 项 P2 尾期需求；PRD 的纯 P2 汇总 32 项另加 FR-F07 混合期次。FR-H08 按 PRD 单列商用化，不混入 P2。`已接回`表示源码及相应自动化存在，不等于当前浏览器/外部环境全部重新验收；此处不据此倒算阶段完成率。

| ID | 当前状态 | 后续工作或验收边界 |
| --- | --- | --- |
| FR-A09 | 已接回 | 多客户分组/批量导入；重建后界面复验 |
| FR-A10 | 已接回 | 人力记录归集及 R-004 证据；重建后界面复验 |
| FR-A11 | 已接回 | 合同/履约/四流勾稽；重建后界面复验 |
| FR-A12 | 已接回，外部待验 | 视觉提取协议和核对流程；当前真实模型、真实扫描材料效果另验 |
| FR-A13 | 本机仿真自动化通过 | 沙箱采集，不代表真实财务/税务系统接入 |
| FR-B09 | 已接回 | 10 项图规则专项通过；重建后图谱界面复验 |
| FR-B12 | 已接回 | 4 项规则版本专项通过；重建后版本编辑/历史界面复验 |
| FR-C05 | 已接回，部分界面复验 | 14 项通知专项通过；本轮通知列表可用，完整订阅/退订界面复验与生产送达另验 |
| FR-C06 | ✅ 本轮已验收 | 9 项跨期专项、浏览器自动/手动基期及快照跳转、重启复现 |
| FR-C07 | ✅ 本轮已验收 | 10 项专项；真实浏览器证据定位/单位/零分母/同比、迟到响应保护、原会话重启与备份恢复；提示不替代正式评分 |
| FR-D06 | ✅ 本轮已验收 | 10 项归档专项；检索/分页、版本界面、真实 PDF、服务重启和备份恢复通过 |
| FR-D07 | ✅ 本轮已验收 | 12 项专项（含 20 户完整规则合并）、真实浏览器、3 页 PDF 目检、重启与备份恢复 |
| FR-D08 | ✅ 本轮已验收 | 10 项专项、浏览器原件匹配/错版本拒绝、13 页 PDF 目检、重启字节一致和备份恢复；水印不是防转发保证 |
| FR-E01 | ✅ 本轮已验收 | 24 规则反向生成、完整联动答案、原子保存；15 项专项、72 组 Excel 重导入、真实浏览器教学闭环、8 表目检及重启/权限/备份恢复通过 |
| FR-E08 | ✅ 本轮已验收 | 班级/名册、组卷/发布/撤回/截止与作业进度界面；18 项专项、237 项全量，真实浏览器 30/70 分值汇总、到期保护、移出/恢复成员、单案例兼容、迟到响应与重启/备份恢复通过 |
| FR-E09 | 待实现 | 班级薄弱项和逐题统计 |
| FR-E10 | 待实现 | 学生跨次作业能力画像 |
| FR-E11 | 待实现 | 错题沉淀与重练 |
| FR-F07 | 待专项验收/补齐 | 现有机构/角色/客户隔离基础不直接代替完整多租户要求 |
| FR-F10 | 待补齐 | 一致性备份已有；加密、保留期与销毁流程未齐 |
| FR-F12 | 部分实现 | 登录/setup 等防护已有；新增注册/邀请/OAuth 路径逐项补齐 |
| FR-F13 | 部分实现 | 全账号基本强度校验已有；邮箱本地部分等注册策略待补 |
| FR-G10 | 部分实现 | 创始码注册已有；机构成员邀请、完整席位与停用流程待补；保留当前未提交注册改动 |
| FR-G11 | 部分实现 | 验证码/密码重置已有；魔术链接与发起会话绑定等待补，真实发信另验 |
| FR-G12 | 待实现 | 第三方 OAuth、身份归一、绑定与解绑；外部应用凭证需另行提供 |
| FR-G13 | 待补齐 | 成员管理、邀请链接状态/刷新/停用；当前未提交面板改动另验 |
| FR-H01 | 待实现（P2 尾期） | 通知渠道抽象、适配器与渲染器，保留现有队列/幂等/状态机及邮件链路 |
| FR-H02 | 待实现（P2 尾期） | 飞书长连接机器人、摘要卡片与动作回调；真实应用凭证和联调另验 |
| FR-H03 | 待实现（P2 尾期） | 钉钉 Stream 机器人及卡片回调；真实应用凭证和联调另验 |
| FR-H04 | 待实现（P2 尾期） | 外部身份双向确认、多渠道绑定/解绑，不按昵称或手机号自动匹配 |
| FR-H05 | 待实现（P2 尾期） | 消息内一次性、短期授权链接，不直接发送报告文件或长期可访问链接 |
| FR-H06 | 待实现（P2 尾期） | 平台无关动作集与渠道渲染分离，复用工作台业务权限 |
| FR-H07 | 待实现（P2 尾期） | 用户主动投递材料、来源留痕、复用解析链路，不跳过核对与权限 |

当前接续：FR-E09 班级学情统计（班级薄弱项、逐题正确率排名），仍待实现；FR-E08 已完成本轮验收。教师人工评分一致率继续暂缓，不混同于内部功能测试。FR-H08 企业微信按 PRD 暂列商用化：主体资质、服务器/域名及平台合规前置条件须另行核验，当前未接入，不伪造外部验收。

## 本轮工程验收与最终审查

2026-09-26 · FR-C06：对**已冻结 Finding**比较，不重新套用当前 YAML。自动基期按业务期间而非上传时间选择；期间别名归一、每期最新保存版本优先，支持显式选择更早期间及其旧审计版本。主体须同机构、同客户档案、同税号；不混用同名企业，也不混用无客户档案的审计与有档案的审计。基期须更早、同粒度、互不重叠；缺期标记中间变化未知。

- 同口径 `pass→hit` 为新增，`hit→pass` 为消失；两期均命中时只对有序的差额/偏离度或同参考区间之外距离比较恶化/改善。不以此推断税款或法律风险等级。复合/图规则没有单一有序值时只显示持续命中。
- 某期缺规则/未执行不算消失；规则版本/定义、参考区间值变化单独标记口径变化。首期没有可比基期，不把所有命中都称作新增。
- API 复用当前机构/客户权限，并先验证显式基期的访问权限，再检查可比性；会计重指派后旧审计比较不可访问。跨机构不能借基期参数探测数据。
- 全量 **162 项测试**、24 条规则 **96/96 样例**通过。最后调整后，跨期/版本/通知/注册联合 **36 项**复验通过。
- 全新临时库真实浏览器验证默认最新基期显示新增、切换旧重审版本显示偏离恶化、筛选不可比项、打开基期快照、首期不推断新增；服务重启后会话/审计与相同比较恢复。通知列表也已复验，控制台 0 error/0 warning；无真实 AI/邮件调用。
- 本项代码自审检查了规则口径、重复期、权限与页面异步竞态，补齐了手动旧版本选择和防止迟到响应覆盖当前审计；验收截图 `output/playwright/c06-changes-20260926.png` 保留，测试库与临时浏览器记录验收后清理。既有注册、toast、工作台改动未回退或覆盖。

2026-09-26 · FR-D06：新增审计与 v1 HTML/品牌/规则/审计哈希清单在同一事务保存；旧库只新增归档表，不补造以前已存在的报告版本。旧审计首次归档记录实际时间，报告内日期延续该审计的快照日期，与实际归档/首次 PDF 导出时间分开追踪。

- 历史页按企业/税号/审计编号、业务期间、审计日期与命中/高风险筛选，支持分页；关键词按文字匹配，`%`/`_` 不成为通配符。计数与页内容在同一读取事务内获得。
- 同内容复用原版本；品牌、Logo、模板或已校验叙述变化产生新版本，规则判定来自冻结 Finding，不套用当前 YAML。显式 `version` 返回原 HTML；第一次 PDF 导出存档，此后返回相同字节，不重新渲染。版本列表展示时间、操作人、规则版本及 SHA-256。
- 归档写入串行分配版本/去重，PDF 首个完成的导出者胜出，不覆盖；损坏的显式版本拒绝读取并提示核查备份，不静默再生成。SHA-256 是完整性核对，不等同于第三方认证或防伪签名（当时 FR-D08 待做；本轮闭环见下方 D08 记录）。
- 新入口沿用机构/角色/客户当前权限：学生拒绝，跨机构与会计客户重指派后不可访问；会计 PDF 导出仍须二次确认。PDF 生成失败清理自己的临时文件，页面提示错误不离开历史页。
- **10 项归档专项**覆盖事务回滚、旧库迁移、备份恢复、并发去重/版本分配、PDF 首写冻结、篡改拒绝、模板/品牌/叙述变化、检索和权限。当前全量 **172 项测试**与 **96/96 规则样例**通过；归档/跨期/叙述/运维联合 **29 项**通过，Python 编译、依赖与修改页 JS 语法检查通过。
- 全新临时库真实浏览器验证 23 条历史的分页、编号检索、旧审计首次归档、品牌变化生成 v2、两版 HTML/PDF 入口与下载；两版 PDF 各 8 页由 Poppler 全页渲染目检。服务重启后原会话可读取 v1，下载 PDF 与重启前 SHA-256 相同。控制台 0 error/0 warning；无真实 AI/邮件调用。界面截图 `output/playwright/d06-archive-20260926.png` 保留。
- 本项自审检查了权限、冻结数据、事务并发、失败清理及备份迁移；不会代替下面的最终全量审查。新增 H 模块同步为待办，当前注册、toast、趋势图和生态集成文档改动保留。验收服务/浏览器已关闭，本轮临时测试库、下载 PDF 与渲染中间文件已清理，仅保留界面截图。

2026-09-26 · FR-D07：机构总览从同一读取事务捕获当前机构客户、历史审计与品牌。按同机构客户 ID + 税号匹配，每户只选一份冻结审计：默认最新业务期间，同结束日优先较短期间；指定期间归一别名，同期采用最新保存版本，不把后补旧期当作最新业务期。未关联档案、主体不一致和无法识别期间单独计数，不按同名企业合并。

- 客户勾选预览同步重算；空选择禁止生成。命中按冻结等级分组，缺材料与无匹配审计分别说明，均不算全部通过；不相加财务金额，不比较混合期间/规则口径的命中率，不替代客户知识库或逐户证据复核。
- 机构管理员与平台管理员只能读取自己的机构总览；会计/教师/学生拒绝，跨机构报告/客户 ID 不可探测。报告冻结所选客户、审计 ID/内容哈希、规则定义、品牌、模板与 HTML；PDF 首次导出后保存原字节，首写并发不覆盖，损坏拒绝读取而非重建。仅新增表，不伪造旧机构报告。
- **12 项专项**覆盖期间、版本/定义区分、主体、角色/机构权限、20 户完整 24 规则合并、HTML 转义、冻结读取、真实 A4 PDF、并发首写、迁移、备份恢复与失败清理。模板自审发现 `clear` 与字典方法冲突，已改为键访问，并补 HTML/PDF 数字回归；修复后生成新报告，不覆盖已归档内容。
- 当前全量 **184 项测试**、24 条规则 **96/96 样例**通过，Python 编译、依赖及当前页面/工作台/图谱 JavaScript 语法检查通过。20 户测试证明完整合并与计数，不冒充真实机构数据或生产环境性能验收。
- 临时库浏览器实测三户、最新期间/1 月别名、客户选择/全选/空选择、命中/缺材料/无审计/通过状态、客户证据跳转及机构 HTML/PDF 下载。修正后 PDF 共 3 页，由 Poppler 全页渲染目检；服务重启后原会话可读，PDF SHA-256 与重启前相同。控制台 0 error/0 warning（有浏览器表单结构 verbose 提示）；未调用真实 AI 或发信。截图 `output/playwright/d07-org-overview-20260926.png` 保留，验收服务/浏览器已关闭。
- 本项自审覆盖权限、期间选择、冻结口径、客户端选择汇总和 PDF 失败路径。现有注册/验证码、toast、趋势图及新邀请码/知识库隔离产品要求保留；本项不代替完整 P2 与最终审查。
- 本项临时测试库、下载 PDF、渲染中间图和浏览器记录已清理，只保留界面截图；没有删除用户数据库或旧仓库备份。

2026-09-26 · FR-D08：单客户 HTML/PDF 添加客户名与报告快照日期水印；机构报告添加机构/客户数量/生成日期水印，逐户卡片另列客户名与报告日期。逐页打印页眉包含追溯标识，避免正文固定标识在末页被裁切。报告日期与实际归档/首次导出时间分开记录，不改写历史日期。

- 追溯标识绑定完整冻结清单，不把可能重复的人工报告编号作为唯一身份。同内容继续去重；品牌/模板/审计清单变化生成新标识与新版本。登记与归档同一事务保存，重复标识不覆盖。首次 PDF 导出仍冻结原字节，查询/核验不触发重新渲染。
- 登录后查询登记信息不等于核验文件；选择 HTML/PDF 原件后，浏览器本机计算 SHA-256，只发送格式与指纹，不上传报告内容。相同原件匹配；改动、重新保存或选错版本不匹配，不据此推断修改原因。水印/标识可复制，不是第三方数字签名、税务鉴证或防转发保证，也不提供公开报告查询。
- 沿用当前机构、角色及会计客户指派权限；学生、未登录、跨机构与客户重指派后拒绝。机构报告仅本机构管理员角色可核验。完整性损坏或标识绑定不一致返回错误，不静默重建。旧库只新增登记表，不补造旧标识、不覆盖旧 HTML/PDF；CLI 默认有水印，但本地 TPL 标识明确未登记服务器原件。
- **10 项专项**覆盖转义/确定性、报告编号冲突、新版本、仅登记与实际核验区分、复制标识/字节变化拒绝、未导出 PDF、权限/指派、旧库迁移、损坏拒绝、事务回滚、真实逐页 PDF 与备份恢复。当前全量 **194 项测试**、24 条规则 **96/96 样例**通过，依赖、52 个 Python 文件语法及当前页面/工作台/图谱 JS 语法检查通过。现有 Starlette 测试客户端 cookie 弃用警告不影响通过，不宣称测试完全无警告。
- 临时合成库真实浏览器验证单客户与机构报告的登记查询、HTML/PDF 下载及本机文件核验；最终机构 HTML/PDF 匹配、用另一份单客户 PDF 配同一机构标识不匹配。服务停止再启动后原会话继续核验，机构 PDF 重下载 SHA-256 不变。单客户 8 页与最终机构 5 页由 Poppler 全页渲染目检；13 页均有标识，坐标位于页面内。控制台 0 error/0 warning，仅有表单结构 verbose 提示；没有真实 AI/邮件调用。
- 本项自审覆盖原件信任边界、事务登记、权限、旧版兼容、HTML 原始字节下载、页面迟到响应和打印页眉。使用 Playwright 浏览器与 PDF 全页检查补充自动化证据；不代替最终全量审查。截图 `output/playwright/d08-original-verification-20260926.png` 保留；既有注册、验证码、工作台、产品要求及 `.mimosa/` 未回退或清理。
- 验收服务与浏览器已关闭；本轮明确列出的临时合成数据库、下载 HTML/PDF、渲染 PNG 与浏览器记录已清理，未删除用户数据库或其他工作文件。

2026-09-26 · FR-E01 本轮完成：新增不依赖模型或真实客户材料的规则反向求值。读取已启用且覆盖完整教学年度的冻结规则；支持现有三类逻辑、公式与复合规则，参考值保持不动，金额/人数/分母约束后由同一引擎复核。无来源映射、无解或目标未命中时拒绝生成，不预填人工答案。

- 24 条规则 × 3 档偏离幅度 × 3 个种子，216 组核心生成复核通过；标准答案包含全部联动命中，不隐藏与目标规则之外的实际风险。15 项核心/API 专项通过，包括教师发布/学生评分、答案字段隔离、固定编号选项、角色与机构权限、事务回滚、冻结读取和备份恢复。
- 生成参数与审计/报告在同一事务保存，并记录生成器版本、种子、修改指标、完整答案及材料/规则指纹。教师可查看冻结出题记录；学生可读取报表与补充底稿原始指标，不能访问生成参数/答案接口。历史与连续年度底稿写明实际来源期间。
- 教师与获发布作业的学生可下载不含答案的标准 Excel，共 8 表；不导出出题参数、隐藏答案、宏或公式。金额/比例为数值、编码为文本、人数为整数，历史来源期间分开列明；下载前用实际解析器核对企业、科目、申报及全部指标，精度损失即拒绝。24 规则 × 3 档幅度共 72 组重导入后数值与引擎答案一致。当前锁定运行环境下重复下载字节稳定；不是永久冻结导出版本或第三方签名。
- 临时合成库真实 Chrome 验证教师 R-020/种子 17 生成、下载、发布及冻结记录读取，完整答案为 R-008/R-020/R-021；学生答题前读取 66 项指标且无教师答案/参数，教师结果 DOM 为空。故意选 R-001 得 0 分、漏检 3 项/误报 1 项并有计算依据；选择三项完整答案得 100 分，教师可见持久化提交。服务重启后原会话/作业恢复，学生下载与重启前教师下载 SHA-256 相同。正常流程无控制台 error/warning；学生主动请求教师答案产生预期 403 网络错误，另有既有表单结构 verbose 提示。
- 本项自审发现学生选项继承审计命中优先排序会提示答案，已改固定按编号排序并补断言；下载沿用发布、指定学生、机构与角色权限，撤回作业即不可下载，损坏记录拒绝读取。表格技能用于只读导入/扫描与 8 表全内容渲染目检，现有应用 Excel 依赖未替换为工作站工具。截图 `output/playwright/e01-student-score-20260926.png` 保留；本项自审不代替最终全量审查。
- 当前全量 **209 项测试**、24 规则 **96/96 样例**、依赖、64 个 Python 文件及 5 个 JS 脚本语法检查通过；现有 cookie 弃用警告仍在。未调用真实 AI、邮件或使用真实企业数据。验收服务/浏览器已退出，29 个明确临时文件、QA 依赖链接和空目录已清理，仅保留评分截图；未删除用户运行数据库或依赖目录。

2026-09-26 · FR-C07：在现有只读查账与风险标注沙箱中加入即时教学反馈。选择风险后列出冻结规则要求的原始输入、来源与口径，并提示未定位/缺少证据；原始指标支持有方向差额和比例试算。缺项不当零、零分母不算 0%、人数/金额/比例不混算；同期间与上年同期显式区分，历史实际期间来自来源说明，不从指标名字猜测。未知历史期间、隔年和不同粒度拒绝给出合格试算数值。

- 提示仅读取作业冻结的规则定义与原始 Dataset，不读取 Finding 的命中/通过、测量值、结论或计算答案，不重跑当前规则或提前评分。定位/试算只在当前页面使用，不保存答案、不修改材料；正式勾选提交与评分仍沿用原有链路。当前沙箱没有凭证编辑功能，不以这些提示冒充任意记账动作校验。
- **10 项专项**覆盖全部 24 条规则的证据定位、缺失/非法数值/零分母/单位/期间、图路径提示、角色/机构/指定学生/撤回权限、冻结 Finding 状态扰动仍返回相同提示、备份恢复与旧材料兼容。全量 **219 项测试**、**96/96 规则样例**、依赖、65 个 Python 文件及 5 个 JS 脚本语法检查通过；最后的前端错误保护补丁另经浏览器与 JS 语法复验。
- 合成库真实 Chrome 验证 R-020 四项来源证据勾选不混入正式答案；分母零、人数与金额混用、跨期误比即时阻止；成本/收入显示 `0.5（50.0%）`，正确同比显示 `1（100%）`。延迟旧提示、旧作业打开响应均未覆盖最新页面；自审补齐旧作业提交失败的错误提示保护，并用延迟模拟 422 复验。B 作业原有 100 分提交保留，服务重启后原会话可用、同一反馈 JSON 完全一致。
- 来源卡片已改为独立纵向布局，四项卡片宽度均在面板内；正常流程监听为 0 error/0 warning。早期停止验收服务后的连接失败、专门模拟 422 的网络错误与既有表单 verbose 提示单独记录，不混同正常流程。未调用真实 AI/邮件。仅保留截图 `output/playwright/c07-teaching-feedback-20260926.png`；验收服务/浏览器已退出，合成测试库及临时浏览器记录已清理，用户运行数据与原暂存/未暂存改动保留。
- 本项自审不替代最终全量代码审查；下一项 FR-E08，完整 P2 与教师外部验收仍未完成。

2026-09-26 · FR-E08（后端与界面均已完成本轮验收）：新增教师所属班级、有效学生名册与版本冲突保护；学生只读自己的班级，不返回同班他人的名册。可将 1–30 份明确标记的仿真案例按顺序和分值组卷，题目沿用各自冻结的规则、完整风险答案及独立评分；未全部提交时总评分保持未完成，全部完成后按题目分值加权，教师复核分数纳入汇总。

- 新建试卷、题目和作业设置在一个事务内完成，任一案例无效全部回滚。组成与分值在创建后冻结；改变题目须新建试卷，不回写已评分旧卷。统一发布/撤回与修改截止时间原子更新全部题目，拒绝单题绕过试卷设置；名单与发布设置需匹配当前 revision，并发编辑只允许一个成功。
- 截止时间必须带时区，统一存 UTC；可显式设为无截止。发布时截止必须在未来；到点后禁止提交或覆盖成绩，但仍可查看已获授权材料、反馈与自己的历史结果。提交前和 `BEGIN IMMEDIATE` 写入事务内均复核发布、机构、班级成员、账号有效性和服务器时间，防止在评分期间撤回/移出班级/到点后的陈旧请求写入。移出班级或撤回后，列表、详情、下载、提示与提交同时不可访问，历史提交保留。
- 旧单案例作业、原始提交和评分保持兼容；迁移仅新增表，不补造班级或截止时间。单作业也支持班级/指定学生交集、截止和发布管理；新班级/试卷题目仅其所属教师可管理与查看提交，旧机构级作业的既有教师共享读取边界保留，不冒充完整多租户专项验收。
- 自审发现原学生作业接口返回的自定义命中权重可提示答案，已移除学生列表/详情中的权重字段，实际评分仍读取服务器配置；非法非有限分值的验证错误响应不再回显原始输入，避免 NaN/Infinity 引起响应序列化失败或回显表单敏感值。
- **18 项专项通过**：班级/角色/机构权限、名册原子替换、草稿/多题发布、30/70 分值汇总与复核、UTC/精确截止边界、所有学生路径撤权、失败回滚、非有限参数、单题绕过拒绝、提交写入二次校验、旧库原始行不变、并发 revision 冲突、备份恢复后原会话/试卷/成绩相同，以及新前端资源/入口返回。使用独立临时合成库，无真实 AI/邮件。
- 教师界面支持新建/选择班级、编辑名册、案例选入/移除/调序/分值、明确选择班级或全机构范围、草稿保存、发布/撤回及截止调整；单案例链路兼容。刷新列表保留未保存组卷与名册编辑；版本变化要求刷新而不静默覆盖。学生可查看班级、试卷列表、逐题得分/完成数、加权总评分及截止状态。未全部提交不冒充最终成绩，服务器仍是截止与权限的最终依据。
- 真实 Chrome 三个独立会话验证：教师建班与 30/70 分值双案例草稿，学生看不到草稿，未入班学生看不到发布试卷；发布后第一题全对 100 分、第二题空答 0 分，总评分 30 分，教师看见持久化提交。撤回与移出班级后刷新均隐藏已开试卷/材料/成绩，重新发布/入班后历史分数保持。单案例草稿发布和截止信息可用；修改截止至近期，到点后按钮自动禁用，直接 POST 返回 409，原零分提交完全未被覆盖；可延长截止，秒级设置不被编辑框截断。
- 自审补齐刷新列表时取消旧试卷/旧案例详情响应，浏览器通过延迟响应验证旧页面不再出现；修复旧提交错误保护继续保留。服务实际停止/重启后，师生原会话、班级/试卷/作业完整 JSON、教师提交列表均与重启前相同，学生总评分仍 30。正常流程监听 0 error/0 warning；故意到期提交的预期 409 网络错误与既有表单 verbose 提示单列。
- 当前全量 **237 项测试**、24 条规则 **96/96 样例**、依赖、67 个 Python 文件及 6 个 JS 脚本语法检查通过；既有测试客户端 cookie 弃用警告仍在。Playwright 用于真实交互和两端截图目检，保留 `output/playwright/e08-teacher-classroom-20260926.png`、`output/playwright/e08-student-progress-20260926.png`；三份验收浏览器及服务已关闭，24 个明确临时测试文件已清理，用户运行数据和原有改动保留，未提交代码。
- 下一项 FR-E09 班级学情统计；本项自审不替代最终全量代码审查，完整 P2 与教师外部评分一致率仍未完成。

- [ ] P1/P2 目标全部完成后，按用户要求进行最终代码审查：覆盖当前差异及相关调用链、权限、泄露、事务并发、迁移、失败路径与历史兼容；修复并回归，汇报未解决项。阶段自审不替代最终审查。

## 外部或生产验收

| 验收 | 状态与需要的证据 |
| --- | --- |
| Docker 镜像构建/运行 | Compose 配置通过；本机 daemon 未运行，须真实构建并启动健康容器 |
| 新仓库部署与 HTTPS | 尚未部署；旧项目的 TLS/VPS 记录不继承到此仓库 |
| 真实模型提取与叙述 | 未使用真实密钥；当前验证仅证明协议、单位/证据/引用校验与失败处理 |
| 真实邮件投递 | 未发信；当前验证仅证明本机重置流程和模拟邮件调用 |
| 教师人工评分一致率 | 未组织真实教师验收；自动化评分不能代替 |
| 敏感字段加密、环境隔离和完整多租户 | 继续依据原 PRD 设计和验收；现有机构/角色权限不代替这些能力 |

当前工作目录为 `D:\CodeWorkspace\TaxPearls`。重建记录中的旧备份路径 `D:\CodeWorkspace\TaxPearls-before-switch-20260926` 在 2026-09-26 复查时已不存在，不视为当前可恢复备份；本轮未删除该目录。只接回业务源码和测试，旧 Git 历史、运行数据及被排除材料未导入。
